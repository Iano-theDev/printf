#include "main.h"

/**
 * parse_percent - converts % to a string and returns it
 * @ap: pointer to string arg
 * Return: string pointer
 */
char *parse_percent(va_list ap)
{
	char *dest = "%";
	(void)ap;
	
	return (dest);
}
